$schema: "http://json-schema.org/draft-04/schema#"

description: snakemake metadata file

type: object

properties:
  contrast:
    type: string

  file:
    type: string

required:
  - contrast
  - file

