---
title: "Untitled"
author: "icervenka"
date: "22/07/2020"
output: html_document
---

```{r setup}

```

```{r}
  mpg %>% ggplot(aes(x = cty, y = hwy)) + geom_point()
```

```{r}
csv_path = paste0("../../csv/", outdir, "/")
dir.create(csv_path, showWarnings = T)
map_if(all_cp,
       is.not.empty.list,
       ~ create_pathway_csv(.x, template, csv_path),
       .else = NULL)
```